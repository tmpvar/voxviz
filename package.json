{
  "dependencies": {
    "chokidar": "^2.1.2",
    "glob": "^7.1.1",
    "glsl-extract": "^1.0.0",
    "glslify": "^6.0.1",
    "mkdirp": "^1.0.4",
    "ndarray": "^1.0.18",
    "ndarray-fill": "^0.1.0",
    "yargs": "^16.2.0"
  },
  "scripts": {
    "glsl-reload": "node shaders/convert.js -w -o build/shaders",
    "generate": "rm -rf build && mkdir build && cd build && cmake -G \"Visual Studio 15 2017\" -A x64 -T host=x64 .."
  }
}
